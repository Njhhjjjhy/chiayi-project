# dependencies
node_modules/

# build output
dist/

# environment
.env
.env.local

# editor
.vscode/
.idea/
*.swp

# os
.DS_Store

# showcase (portfolio docs, not part of codebase)
showcase/
.vercel

# PBR textures (265MB, downloaded reference material — kept local, not deployed)
reference/textures/

# Research-trip reference videos (150MB — kept local, frames extracted to frames_views/)
reference/research-trip-20260418/*.mp4

# AI tool artifacts
.agents/
.claude/
.windsurf/
.handoffs/
skills-lock.json

# QA panel local dev data (test notes + screenshots)
.qa-dev-notes.json
public/qa-dev-screenshots/

# Baseline screenshots (local visual diagnostics, not versioned)
baselines/

# Local-only reference image dumps (kept on disk, not pushed)
design-references/
qa/
meeting-refrence-*.webp

# Concept image briefs (64MB of PNGs — kept local; docs/canonical/11 maps them)
concept-images/

# Hand-gesture webcam demo (2.6GB local experiment, has its own HANDOVER.md)
3d-image/
