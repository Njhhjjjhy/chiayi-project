// Stubbed for v2. The L-shaped pathway is now formed by the three
// partitions in EntranceWallPartition.jsx, the back-wall, and the
// window-wall — no separate partition geometry lives here.
//
// Information card placeholders and pathway point lights will be
// designed fresh in the new coordinate system later. Until then this
// component renders nothing.

export default function Pathway() {
  return null
}
